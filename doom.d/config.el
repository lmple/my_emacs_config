;;; $DOOMDIR/config.el -*- lexical-binding: t; -*-

(setq user-full-name "Lièvre Loïc"
      user-mail-address "llievre@protonmail.com")

(setq doom-theme 'doom-spacegrey)
(setq display-line-numbers-type t)
(setq org-directory "~/org/")
